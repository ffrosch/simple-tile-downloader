{
  "name": "simple-tile-downloader",
  "version": "0.4.1-alpha",
  "type": "module",
  "author": "Florian Frosch",
  "dependencies": {
    "lodash": "^4.17.21",
    "ol": "^10.6.1"
  },
  "peerDependencies": {
    "typescript": "^5.9.3"
  },
  "devDependencies": {
    "@happy-dom/global-registrator": "^20.0.8",
    "@types/bun": "^1.3.0",
    "@types/lodash": "^4.17.20",
    "happy-dom": "^20.0.8"
  },
  "description": "Download XYZ tiles from a service given a bounding box and the target zoom levels.",
  "keywords": [
    "xyz",
    "tile",
    "download",
    "map",
    "promise"
  ],
  "license": "ISC",
  "scripts": {
    "build": "bun build ./src/index.ts --outfile=dist/index.js --target=browser --packages=external --production && bun run build:declaration",
    "build:declaration": "tsc --project tsconfig.types.json",
    "publish": "bun run build && bun publish",
    "test:docker:up": "docker compose -f docker/compose.yml up -d --wait",
    "test:docker:down": "docker compose -f docker/compose.yml down",
    "test:docker": "bun run test:docker:up && bun test tests/integration && bun run test:docker:down",
    "test:all": "bun run test:docker:up && bun test && bun run test:docker:down"
  },
  "exports": {
    ".": {
      "import": "./dist/index.js"
    }
  },
  "module": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "files": [
    "dist"
  ]
}
